version: '3.8'

services:
  neuro-symbolic-rag:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3003:3003"
    environment:
      - PORT=3003
      - NODE_ENV=production
    restart: unless-stopped
